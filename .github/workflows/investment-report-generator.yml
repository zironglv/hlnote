name: Investment Report Generator

on:
  workflow_dispatch:
  schedule:
    - cron: '0 23 * * *'

jobs:
  generate-investment-report:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run investment report generator
      env:
        DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
      run: |
        echo "🚀 Starting investment report generation..."
        echo "🔍 使用调试版本以诊断问题..."
        python main_multi_debug.py
        REPORT_EXIT_CODE=$?
        echo "Report generation exit code: $REPORT_EXIT_CODE"
        if [ $REPORT_EXIT_CODE -eq 0 ]; then
          echo "✅ Report generation completed successfully"
        else
          echo "⚠️ Report generation completed with warnings (exit code: $REPORT_EXIT_CODE)"
          # 即使有错误也继续执行，因为我们想要生成报告
        fi
        
    - name: Upload artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: investment-report-results
        path: |
          reports/
          *.log
        if-no-files-found: ignore