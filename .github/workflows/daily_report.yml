name: AI投研助手 - 多指数每日报告

on:
  schedule:
    # 每天UTC时间23:00执行（北京时间次日早上7点）
    - cron: '0 23 * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  generate-report:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout代码
      uses: actions/checkout@v4
      
    - name: 设置Python环境
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'
        
    - name: 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        # 确保安装openpyxl用于读取Excel文件
        pip install openpyxl
        
    - name: 配置环境变量
      run: |
        echo "使用钉钉机器人推送，无需邮箱配置"
        echo "配置检查完成"
        echo "Python版本: $(python --version)"
        echo "pip版本: $(pip --version)"
        
    - name: 配置钉钉Webhook（可选）
      run: |
        if [ -z "$DINGTALK_WEBHOOK" ]; then
          echo "⚠️ 注意: DINGTALK_WEBHOOK 环境变量未设置"
        else
          echo "✅ DINGTALK_WEBHOOK 已设置，长度: ${#DINGTALK_WEBHOOK} 字符"
          # 显示URL的部分信息用于调试（隐藏敏感部分）
          WEBHOOK_DOMAIN=$(echo $DINGTALK_WEBHOOK | cut -d'/' -f3)
          echo "🔗 Webhook域名: $WEBHOOK_DOMAIN"
        fi
        
    - name: 运行多指数AI投研助手
      env:
        DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
      run: |
        echo "🚀 开始运行多指数分析..."
        # 启用详细错误捕获
        set -x  # 显示执行的每条命令
        python main_multi_fixed.py 2>&1 | tee execution_log.txt
        EXIT_CODE=${PIPESTATUS[0]}
        set +x
        echo "程序退出码: $EXIT_CODE"
        if [ $EXIT_CODE -eq 0 ]; then
          echo "✅ 多指数分析执行成功"
        else
          echo "❌ 多指数分析执行失败，退出码: $EXIT_CODE"
          echo "=== 详细错误日志 ==="
          cat execution_log.txt
        fi
        # 不直接退出，让工作流继续执行以便收集日志
        
    - name: 检查报告目录是否存在
      run: |
        if [ -d "reports" ]; then
          echo "REPORTS_EXIST=true" >> $GITHUB_ENV
        else
          echo "REPORTS_EXIST=false" >> $GITHUB_ENV
        fi
      
    - name: 上传报告目录作为artifact
      uses: actions/upload-artifact@v4
      if: env.REPORTS_EXIST == 'true'
      with:
        name: daily-report-${{ github.run_number }}-reports
        path: reports/
        retention-days: 7
        
    - name: 检查日志文件是否存在
      run: |
        if [ -n "$(ls *.log 2>/dev/null)" ] || [ -f "execution_log.txt" ]; then
          echo "LOGS_EXIST=true" >> $GITHUB_ENV
        else
          echo "LOGS_EXIST=false" >> $GITHUB_ENV
        fi
      
    - name: 上传日志文件作为artifact
      uses: actions/upload-artifact@v4
      if: env.LOGS_EXIST == 'true'
      with:
        name: daily-report-${{ github.run_number }}-logs
        path: |
          *.log
          execution_log.txt
        retention-days: 7
        
    - name: 系统状态检查
      if: always()
      run: |
        echo "🖥️ 系统信息:"
        uname -a
        echo "\n📦 Python环境:"
        python --version
        pip list | grep -E "(pandas|requests|matplotlib|openpyxl)"
        echo "\n🌐 网络检查:"
        curl -s -o /dev/null -w "%{http_code}" https://www.baidu.com && echo " 百度访问正常" || echo " 百度访问失败"
        curl -s -o /dev/null -w "%{http_code}" https://oapi.dingtalk.com && echo " 钉钉API访问正常" || echo " 钉钉API访问失败"
      
    - name: 错误日志输出
      if: always()
      run: |
        echo "🚨 输出所有可能的日志内容："
        echo "=== 检查当前目录文件 ==="
        ls -la
        echo "\n=== 检查日志文件内容 ==="
        for logfile in *.log; do
          if [ -f "$logfile" ]; then
            echo "\n--- $logfile 内容 ---"
            # 只显示最近50行，避免日志过长
            tail -n 50 "$logfile"
          fi
        done
        if [ ! -f "multi_index_dividend_analyzer.log" ]; then
          echo "\n--- 没有找到预期的日志文件 ---"
        fi
        
        # 检查是否有报告文件生成
        echo "\n=== 检查报告文件 ==="
        if [ -d "reports" ]; then
          echo "报告目录存在:"
          find reports -type f -name "*.html" -o -name "*.png" | head -10
        else
          echo "报告目录不存在"
        fi